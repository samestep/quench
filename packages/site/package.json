{
  "name": "@quench-lang/site",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "yarn build:public && vite --port=3001",
    "build": "yarn build:public && vite build",
    "build:public": "node build.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@quench-lang/core": "0.3.0",
    "@quench-lang/tree-sitter": "0.3.0",
    "web-tree-sitter": "^0.20"
  },
  "devDependencies": {
    "vite": "^2"
  }
}
