args:
  args:
    - foo
    - bar
  out: |
    [ "foo", "bar" ]

complicated:
  out: |
    foo
    bar
    undefined
    👻 ba # not a comment
    z 🙃
    foo bar baz

errors:
  compile: |
    0:6 to 0:14 Error: syntax (ERROR (string))
    0:24 to 0:24 Error: syntax (MISSING ";")
    Error: Failed to compile.

hello:
  out: |
    Hello, world!
